interface:
  display_name: "SpaceSonar Code Quality"
  short_description: "Review Python, MQL5, data, model, runtime, report, or test code quality."
  default_prompt: "Use $spacesonar-code-quality for non-trivial implementation or review. Check responsibility, flow, contracts, failures, and tests."
policy:
  allow_implicit_invocation: true
