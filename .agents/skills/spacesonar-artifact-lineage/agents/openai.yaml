interface:
  display_name: "SpaceSonar Artifact Lineage"
  short_description: "Track artifact inputs, outputs, paths, hashes, manifests, availability, and lineage."
  default_prompt: "Use $spacesonar-artifact-lineage when artifacts are created, consumed, moved, ignored, packaged, released, or reported. Record paths, hashes, availability, and lineage."
policy:
  allow_implicit_invocation: true
