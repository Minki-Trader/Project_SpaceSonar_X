interface:
  display_name: "SpaceSonar Code Surface Guard"
  short_description: "Guard code ownership, module boundaries, and run-variant sprawl."
  default_prompt: "Use $spacesonar-code-surface-guard for foundation, MT5, EA, pipeline, model, runtime, or report code changes. Name owner, caller, contract, and variant boundary."
policy:
  allow_implicit_invocation: true
