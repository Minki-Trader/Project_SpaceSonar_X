interface:
  display_name: "SpaceSonar Environment Reproducibility"
  short_description: "Check dependencies, Python, CI, MT5 paths, external artifacts, and local assumptions."
  default_prompt: "Use $spacesonar-environment-reproducibility for dependency setup, tests, CI, clean checkout behavior, MT5 paths, local assumptions, or recovery instructions."
policy:
  allow_implicit_invocation: true
