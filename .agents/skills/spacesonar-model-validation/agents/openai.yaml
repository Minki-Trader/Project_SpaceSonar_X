interface:
  display_name: "SpaceSonar Model Validation"
  short_description: "Validate model, threshold, calibration, split, WFO, overfit, and selection claims."
  default_prompt: "Use $spacesonar-model-validation for model training, threshold selection, ranking, calibration, split, WFO, overfit, or model comparison claims."
policy:
  allow_implicit_invocation: true
