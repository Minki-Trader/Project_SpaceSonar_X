interface:
  display_name: "SpaceSonar Session Intake"
  short_description: "Start a turn by identifying current truth, intent, re-entry mode, family candidates, and routing need."
  default_prompt: "Use $spacesonar-session-intake at the start of repository work to establish current truth, re-entry mode, work item routing need, and final answer filter."
policy:
  allow_implicit_invocation: true
