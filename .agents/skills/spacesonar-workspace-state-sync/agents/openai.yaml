interface:
  display_name: "SpaceSonar Workspace State Sync"
  short_description: "Sync ONNX lab current truth, registers, candidates, handoff state, and archive boundaries."
  default_prompt: "Use $spacesonar-workspace-state-sync for workspace truth, register, candidate, handoff, or archive-boundary updates."
policy:
  allow_implicit_invocation: true

