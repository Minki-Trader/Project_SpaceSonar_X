interface:
  display_name: "SpaceSonar Reference Scout"
  short_description: "Use official or maintained refs for version-sensitive APIs, MT5/MQL5, libraries, or quant methods."
  default_prompt: "Use $spacesonar-reference-scout when project memory is not enough or behavior is version-sensitive. Prefer official docs and maintained sources."
policy:
  allow_implicit_invocation: true
