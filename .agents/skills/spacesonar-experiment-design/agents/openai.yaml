interface:
  display_name: "SpaceSonar Experiment Design"
  short_description: "Design experiments with hypothesis, controls, criteria, evidence, and stop conditions."
  default_prompt: "Use $spacesonar-experiment-design before runs. Record hypothesis, comparison, controls, sample scope, success/failure criteria, evidence, and stop conditions."
policy:
  allow_implicit_invocation: true
