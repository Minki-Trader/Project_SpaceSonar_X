interface:
  display_name: "SpaceSonar Experiment Design"
  short_description: "Design experiments with hypothesis, controls, KPI interpretation, axis choice, evidence, and stop conditions."
  default_prompt: "Use $spacesonar-experiment-design before runs or closeout decisions. Record hypothesis, comparison, controls, changed axes, KPI interpretation plan, surface rotation rationale, sample scope, criteria, evidence, and stop/reopen conditions."
policy:
  allow_implicit_invocation: true
