interface:
  display_name: "SpaceSonar Task Force Review"
  short_description: "Route required Codex Task Force review for protected high-claim surfaces."
  default_prompt: "Use $spacesonar-task-force-review when a formal Task Force review is required. Keep micro-consult advisory and formal review evidence separate."
policy:
  allow_implicit_invocation: false
  invocation_boundary: "explicit user request, formal protected-claim review, policy change, runtime authority, operating promotion, or cross-system handoff only"
