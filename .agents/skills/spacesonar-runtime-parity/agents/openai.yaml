interface:
  display_name: "SpaceSonar Runtime Parity"
  short_description: "Check Python, artifact, ONNX, EA, and MT5 runtime meaning before runtime claims."
  default_prompt: "Use $spacesonar-runtime-parity for MT5, EA modules, runtime packages, model bundles, .set files, tester outputs, handoff files, or Python-to-runtime comparisons."
policy:
  allow_implicit_invocation: true
