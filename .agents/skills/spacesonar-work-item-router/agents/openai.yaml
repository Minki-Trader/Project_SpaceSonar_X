interface:
  display_name: "SpaceSonar Work Item Router"
  short_description: "Route non-trivial work by family, primary skill, support skills, gates, phases, and stops."
  default_prompt: "Use $spacesonar-work-item-router after intake for non-trivial work. Select one family, one primary skill, needed supports, required gates, phases, and stop conditions."
policy:
  allow_implicit_invocation: true
