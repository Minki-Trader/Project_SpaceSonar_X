interface:
  display_name: "SpaceSonar Exploration Mandate"
  short_description: "Keep exploration open without legacy inheritance."
  default_prompt: "Use $spacesonar-exploration-mandate for alpha ideas, variants, sweeps, negative-result memory, or reopening ideas. Separate exploration from operating claims."
policy:
  allow_implicit_invocation: true
