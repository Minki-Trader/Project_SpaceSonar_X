interface:
  display_name: "SpaceSonar Architecture Guard"
  short_description: "Guard policy, skills, agents, paths, encoding, ownership, and architecture changes."
  default_prompt: "Use $spacesonar-architecture-guard for policy, skill, agent, path identity, encoding, code ownership, or architecture changes. Keep records compact and machine-readable."
policy:
  allow_implicit_invocation: true
