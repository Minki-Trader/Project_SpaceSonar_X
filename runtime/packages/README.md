# Runtime Packages

Use one folder per `bundle_id`.

`experiment_bundle.json` is the bundle source of truth.

Bundle existence does not claim runtime authority, economics pass, handoff complete, live readiness, reviewed, verified, pass, or Goal Achieve.
