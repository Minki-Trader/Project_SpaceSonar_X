# Training

Reusable model training and calibration logic.

There is no default model family, objective, output head, threshold, or calibration target.

Selection, scaling, thresholds, and calibration must be train-only fitted artifacts with ids and hashes.
