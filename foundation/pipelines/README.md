# Pipelines

Reusable orchestration entry points for the ONNX lab.

Active pipeline style:

- manifest-centered
- run-id aware
- bundle-id aware when exporting ONNX or runtime packages
- ID-based ownership

This directory is intentionally empty until a new reusable ONNX lab command is needed.
