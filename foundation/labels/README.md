# Labels

Reusable label generation logic.

There is no default label, target, direction mapping, holding period, or horizon.

Keep feature timestamps and target timestamps separate. Every label generator must declare its task surface, horizon or holding logic if any, split-boundary exclusions, and claim boundary.
