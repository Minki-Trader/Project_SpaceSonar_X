# Runs

Each meaningful run should use:

`lab/runs/<run_id>/`

Preferred contents:

- `run_manifest.json`
- `experiment_receipt.yaml`
- `artifact_lineage.json`
- `metrics.json`
- optional ignored `logs/`, `reports/`, and generated `artifacts/`

