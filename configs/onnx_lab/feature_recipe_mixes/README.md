# Feature Recipe Mixes

Feature recipe mixes combine explicit feature recipes and define ordering and collision policy.

They do not define a default feature count, default feature family, or inherited feature surface.
