# Split Recipes

Split recipes declare train, validation, OOS, WFO, purge, and embargo boundaries.

Active split catalog:

- `split_set_v0.yaml`: adopted US100 M5 clean-universe split catalog. It is not a reviewed/pass/selected-baseline claim.
