# ONNX Lab Configs

`configs/onnx_lab/` stores declarative recipe specs.

Executable reusable logic belongs in `foundation/` or `src/`. Run-local resolved evidence belongs in `lab/runs/`. Materialized data belongs in `data/processed/onnx_lab/`.

